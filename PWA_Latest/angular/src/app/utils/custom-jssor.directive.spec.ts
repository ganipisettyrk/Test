import { CustomJssorDirective } from './custom-jssor.directive';

describe('CustomJssorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomJssorDirective();
    expect(directive).toBeTruthy();
  });
});
