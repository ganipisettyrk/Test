import { CustomEllipsePipe } from './custom-ellipse.pipe';

describe('CustomEllipsePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomEllipsePipe();
    expect(pipe).toBeTruthy();
  });
});
