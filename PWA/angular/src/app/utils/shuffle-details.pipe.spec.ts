import { ShuffleDetailsPipe } from './shuffle-details.pipe';

describe('ShuffleDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShuffleDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
